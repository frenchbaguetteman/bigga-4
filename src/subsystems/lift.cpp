/**
 * @file lift.cpp
 * Lift subsystem — CURRENTLY DISABLED (no lift hardware wired).
 * All methods are now inlined in the header as no-ops.
 * This file is kept to satisfy the build system's source listing.
 */
#include "subsystems/lift.h"
